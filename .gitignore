# Ignore all runtime config directories inside docker/
docker/*

# But do track the docker-compose.yml file
!docker-compose.yml

# Ignore logs, databases, and cache files created by the apps
*.log
*.db
*.sqlite
*.cache

# Optional: Ignore any environment files with secrets
.env

